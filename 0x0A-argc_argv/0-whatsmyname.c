#include "main.h"
#include <stdio.h>

/**
 * main - prints a programs name
 * @argc: argument count
 * @argv: argument vector
 *
 * Rerutn: Always zero
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
